// import { supabase } from '$lib/supabase/supabaseClient';

// export async function load() {
// 	const { data: courses, error } = await supabase.from('courses').select('*').eq('id', this.params.id);

// 	if (error) {
// 		console.log(error);
// 	}

// 	return { courses };
// }

